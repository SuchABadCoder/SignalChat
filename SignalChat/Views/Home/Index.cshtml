@model IEnumerable<Chat>

<div class="chat-body">
    @foreach(var chat in Model)
     {
        @Html.ActionLink(chat.Name ?? "Unknown", "JoinRoom", "Home", routeValues: new { id = chat.Id }, htmlAttributes: new { @class = "room-button" })
     }
</div>
